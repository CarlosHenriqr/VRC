/** Feature flags — toggle sections without deleting code. */
export const FEATURES = {
  /** /cases page, nav, home Portfolio, and sector/service case blocks */
  cases: false,
  /** LinkedIn icon/link in footer and contact page */
  linkedin: false,
  /** Displayed company phone number in footer and contact page */
  phone: false,
} as const;
